{# MODIFICATION : Ajout d'un titre pour correspondre à la Photo 2 #}

<h1>add Author</h1>

{{ form_start(formulaire) }}

{# MODIFICATION : Structure pour empiler les champs verticalement #}

{# Ligne pour Username #}

<div style="margin-bottom: 10px;">
{# Label 'Username' (pris par défaut) au lieu de 'votre nom' #}
{{ form_label(formulaire.username, 'Username') }}
{{ form_widget(formulaire.username) }}
{{ form_errors(formulaire.username) }}
</div>

{# Ligne pour Email #}

<div style="margin-bottom: 10px;">
{{ form_label(formulaire.email, 'Email') }}
{{ form_widget(formulaire.email) }}
{{ form_errors(formulaire.email) }}
</div>

{# MODIFICATION : Le champ 'age' est supprimé pour correspondre à la Photo 2 #}

{# Affiche le bouton 'Save' (et le reste du formulaire) #}
{{ form_end(formulaire) }}