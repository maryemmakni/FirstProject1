{{ form_start(formulaire) }}

{{ form_label(formulaire.title, 'Titre') }}<br>
{{ form_widget(formulaire.title) }}
{{ form_errors(formulaire.title) }}<br>

{{ form_label(formulaire.publicationDate, 'Date de publication') }}<br>
{{ form_widget(formulaire.publicationDate) }}
{{ form_errors(formulaire.publicationDate) }}<br>

{{ form_label(formulaire.category, 'Catégorie') }}<br>
{{ form_widget(formulaire.category) }}
{{ form_errors(formulaire.category) }}<br>

{{ form_label(formulaire.author, 'Auteur') }}<br>
{{ form_widget(formulaire.author) }}
{{ form_errors(formulaire.author) }}<br>


{{ form_rest(formulaire) }}

{{ form_end(formulaire) }}
